"""Joker Protocol — optional observation-layer extension (disabled by default).

Phase 2 implements only the data hooks: card lifecycle + false-signal
injection into the opponent's observation. It never mutates true state,
transitions, or scoring.
"""
